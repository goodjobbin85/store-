
<div align="center">
  <h3>Your Cart</h3>
  <%= button_to "Empty Cart", cart, method: :delete, data: { confirm: "Are You Sure?" } %>

  <%= render cart.line_items %>

  <h2>Total Checkout Price: $<%= cart.total_checkout_price %></h2>

  <%= link_to 'Edit', edit_cart_path(cart) %> |
  <%= link_to 'Back', carts_path %>
</div>
